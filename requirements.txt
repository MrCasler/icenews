# Core dependencies for ICENews ingestion
scrapfly-sdk>=0.8.5
jmespath>=1.0.1

# Optional but recommended (used by scraper)
loguru>=0.7.0

# Web app
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
pydantic>=2.0.0

# Testing
pytest>=7.0.0
httpx>=0.25.0
requests>=2.31.0

# Download feature (premium)
yt-dlp>=2024.0.0

# Authentication (magic link + X OAuth)
itsdangerous>=2.1.0
authlib>=1.3.0
python-multipart>=0.0.6

# Email sending (magic links)
resend>=0.8.0

# Environment variables
python-dotenv>=1.0.0

# Payments (Stripe subscription)
stripe>=7.0.0
